const file=document.querySelector('#file'),photo=document.querySelector('#photo'),toast=document.querySelector('#toast');document.querySelectorAll('#pick,.change').forEach(b=>b.addEventListener('click',()=>file.click()));file.addEventListener('change',()=>{if(file.files[0])photo.style.backgroundImage=`url(${URL.createObjectURL(file.files[0])})`});document.querySelector('.remove').addEventListener('click',()=>{file.value='';photo.style.backgroundImage=''});document.querySelector('#form').addEventListener('submit',e=>{e.preventDefault();toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)});document.querySelector('.cancel').addEventListener('click',()=>location.reload());
const layout=document.querySelector('#profile-layout'),drawerToggle=document.querySelector('#drawer-toggle'),drawerOverlay=document.querySelector('#drawer-overlay');function setDrawer(open){layout.classList.toggle('drawer-opened',open);document.body.classList.toggle('drawer-open',open);drawerToggle.setAttribute('aria-expanded',String(open));drawerToggle.setAttribute('aria-label',open?'Fechar menu':'Abrir menu')}drawerToggle.addEventListener('click',()=>setDrawer(!layout.classList.contains('drawer-opened')));drawerOverlay.addEventListener('click',()=>setDrawer(false));document.addEventListener('keydown',event=>{if(event.key==='Escape')setDrawer(false)});
